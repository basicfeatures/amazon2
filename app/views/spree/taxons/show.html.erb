<h2><%= @taxon.name %></h2>
<%= render "spree/shared/products", locals: { products: @products, taxon: @taxon } %>
<% unless params[:keywords].present? %>
  <% cache [I18n.locale, @taxon] do %>
    <%= render "taxon", collection: @taxon.children %>
  <% end %>
<% end %>

